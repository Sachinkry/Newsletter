/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['via.placeholder.com', "storyset.com", "substackcdn.com"],

  },
}

module.exports = nextConfig
